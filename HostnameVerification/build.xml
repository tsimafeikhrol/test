<property name="package.file" value="hostnameVerification.war" />
<property name="temp.dir" value="HostnameVerification" />
<property name="temp.dir.web-inf" value="HostnameVerification/web/WEB-INF" />
<property name="pages.dir" value="HostnameVerification/src" />
<property name="lib.dir" value="HostnameVerification/web/WEB-INF/lib" />

<!-- with all resources in place, create the war file -->
<war destfile="${package.file}" 
     webxml="${temp.dir.web-inf}/web.xml" 
     basedir="${temp.dir}"
  <fileset dir="${pages.dir}"/>
  <lib dir="${lib.dir}" />
</war>